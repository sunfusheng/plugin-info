import org.gradle.api.Plugin
import org.gradle.api.Project

class PluginBuildInfo implements Plugin<Project> {

    private static final TAG = 'PluginBuildInfo'

    @Override
    void apply(Project project) {
        println TAG + project.name + ' apply ' + project.path + ' plugin'
    }
}